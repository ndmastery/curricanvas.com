import { createPageHead } from "@/shared/config/seo.config";

export const changelogHead = createPageHead({
  title: "Changelog — CurriCanvas",
  description: "Explore CurriCanvas product updates, release notes, improvements, and platform changes.",
  path: "/changelog",
  twitterCard: "summary_large_image",
});
