import { createPageHead } from "@/shared/config/seo.config";

export const supportHead = createPageHead({
  title: "Support — CurriCanvas",
  description: "Find answers about CurriCanvas accounts, resume editing, exports, credits, integrations, privacy, custom domains, and AI features.",
  path: "/support",
  twitterCard: "summary_large_image",
});
